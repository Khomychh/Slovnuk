# Залежності підбору персональних параметрів FSRS.
#
# Окремий файл, бо `backend/Dockerfile` копіює `requirements.txt` в образ API
# цілком. `fsrs[optimizer]` тягне torch, numpy, pandas і tqdm — близько двох
# з половиною гігабайтів заради задачі, яка виконується раз на кілька місяців
# на машині розробника (ADR-0002).
#
# Ставиться руками, у той самий venv бекенда:
#
#     pip install -r requirements-optimizer.txt
#
# Версія прив'язана до `fsrs` з requirements.txt: екстра доставляє важкі
# залежності до вже встановленого пакета, і розходження версій тут означало б
# два різні fsrs у одному оточенні.
fsrs[optimizer]==6.3.1
