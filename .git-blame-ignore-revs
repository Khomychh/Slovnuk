# Коміти, які змінили вигляд коду, а не його зміст. `git blame` мусить їх
# пропускати, інакше на кожному переформатованому рядку він показуватиме не ту
# зміну, де писалась логіка.
#
# GitHub читає цей файл сам. Локально ввімкнути один раз:
#     git config blame.ignoreRevsFile .git-blame-ignore-revs

# style: apply ruff autofixes — сортування імпортів у 64 файлах
ad1ae97faa0ecf121f550461f7451268902fcf30

# style: run ruff format — переноси рядків у 57 файлах
fbc0be6492b5a256be875a0fda3e90535b9124c6
